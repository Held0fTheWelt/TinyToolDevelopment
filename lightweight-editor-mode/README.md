# Lightweight Editor Mode

This path matches descriptor `DocsURL` `https://docs.tiny-tool-development.com/lightweight-editor-mode/`.

Canonical category landing: [editor-tools/LightweightEditorMode](../editor-tools/LightweightEditorMode/README.md).

## Available languages

Buyer docs are available under `/en/`, `/de/`, `/fr/`, `/it/`, `/es/`, `/ja/`, and `/zh/` (English remains the default landing).
