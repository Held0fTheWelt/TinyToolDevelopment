# Unified MCP Server

This path matches descriptor `DocsURL` `https://docs.tiny-tool-development.com/unified-mcp-server/`.

Canonical category landing: [ai-plugins/UnifiedMcpServer](../ai-plugins/UnifiedMcpServer/README.md).

## Available languages

Buyer docs are available under `/en/`, `/de/`, `/fr/`, `/it/`, `/es/`, `/ja/`, and `/zh/` (English remains the default landing).
